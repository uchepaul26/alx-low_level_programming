#include "main.h"

/**
 * _strlen - function that calculates the length of a string
 * @s: string to check
 * Return: length of the string
 */

int _strlen(char *s)
{
	int length;

	length = 0;
	while (*s != '\0')
	{
		lenght++;
		s++;
	}
	return (length);
}
