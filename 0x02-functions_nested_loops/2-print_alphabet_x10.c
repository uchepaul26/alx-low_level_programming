#include "main.h"

/**
 * print_alphabet_x10 - prints the lowercase alphabets x10
 */

void print_alphabet_x10(void)
{
	int count = 1;
	char ch;

	while (count <= 10)
	{
		for (ch = 'a'; ch < 'a'; ++ch)
			_putchar(ch);
	}
	_putchar('\n');
}
